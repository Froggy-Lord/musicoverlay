org.gradle.jvmargs=-Xmx2G -Dfile.encoding=UTF-8
org.gradle.parallel=true

# --- Default target: Minecraft 26.2 ---
# Build 26.1.2 instead by overriding on the command line, e.g.
#   ./gradlew build -Pminecraft_version=26.1.2 -Pfabric_api_version=0.154.2+26.1.2 -Ploom_version=1.16-SNAPSHOT
# or just run ./build-all.sh to produce jars for every supported version.
minecraft_version=26.2
loader_version=0.19.3
loom_version=1.16.3
fabric_api_version=0.152.2+26.2

# Mod properties
mod_version=1.0.1
maven_group=com.froggylord
archives_base_name=musicoverlay

# Optional integrations
mod_menu_version=20.0.0-beta.3
